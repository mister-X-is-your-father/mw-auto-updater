# Middleware Upgrade Configuration
# ミドルウェアアップグレード設定

[[middleware]]
name = "php"
current = "8.2"
target = "^8.5"

# Data sources to fetch from (all will be queried independently)
# Available: "github", "php.watch", "local"
sources = ["github", "local"]
